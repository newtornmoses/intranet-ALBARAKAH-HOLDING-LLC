import { ActiveButtonDirective } from './active-button.directive';

describe('ActiveButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveButtonDirective();
    expect(directive).toBeTruthy();
  });
});
