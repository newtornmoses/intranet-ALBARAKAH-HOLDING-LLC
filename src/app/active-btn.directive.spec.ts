import { ActiveBtnDirective } from './active-btn.directive';

describe('ActiveBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveBtnDirective();
    expect(directive).toBeTruthy();
  });
});
